<IfModule mod_rewrite.c>
    RewriteEngine On

    # Non reindirizzare file che esistono nella root (es. install.php)
    RewriteCond %{REQUEST_FILENAME} !-f

    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
